# Sao chép thành .env và điền token (file .env đã gitignore — KHÔNG commit).
# Lấy token: https://www.kaggle.com/settings → API → Generate New Token

export KAGGLE_API_TOKEN=your_kaggle_api_token_here
